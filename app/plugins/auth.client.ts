/**
 * Authentication plugin
 * Ensures auth composables are properly initialized
 */
export default defineNuxtPlugin(() => {
  // Plugin initialization happens automatically
  // The composables are auto-imported by Nuxt
})